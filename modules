 For apache install
ansible demo -b -m apt -a "pkg=apache2 state=present"

For apache remove
ansible demo -b -m apt -a "pkg=apache2 state=absent"

For apache upadate 
ansible demo -b -m apt -a "pkg=apache2 state=latest"

For apache service start
ansible demo -b -m service -a "name=apache2 state=started"

For creating user
ansible demo -b -m user -a "name=sovit"


Description:
-m : modules
presnet: install
ubsent: remove
latest: update
