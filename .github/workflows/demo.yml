name: SonarQube Analysis on:
  push:
    branches:
      - main jobs:
  sonarqube:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: SonarQube Scan
        uses: sonarsource/sonarqube-scan@master
        with:
          sonar-token: sqp_d987a78df4fb9afc8215cf205380d358ae280393
          sonar-project-key: sonargithub
          sonar-project-name: sonargithub
          sonar-host-url: http://localhost:9000
          sonar-sources: .
