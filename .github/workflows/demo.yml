name: SonarQube Analysis on:
  push:
    branches:
      - main jobs:
  sonarqube:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: SonarQube Scan
        uses: sonarsource/sonarqube-scan@master
        with:
          sonar-token: sqp_db593163a011e456650eb0797fc20cb25b68ae8a
          sonar-project-key: sonargithub
          sonar-project-name: sonargithub
          sonar-host-url: http://localhost:9000
          sonar-sources: .
